
# Deploy guard scripts/logs — NEVER public (ZeroHype isolation)
verify_deploy.sh
verify_deploy.log
*.sh
*.log

# Agent config — local only, never deployed (GitHub Pages serves everything committed)
AGENTS.md
.opencode/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
.outreach_build
wrangler.jsonc

# Stale local duplicates of the site llms files (also point at the wrong domain:
# zerohype.co instead of zerohypelab.com). Root-level llms.txt / llms-full.txt
# are the canonical, deployed ones. Never publish both.
public/
