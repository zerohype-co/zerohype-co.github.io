
# Deploy guard scripts/logs — NEVER public (ZeroHype isolation)
verify_deploy.sh
verify_deploy.log
*.sh
*.log

# Agent config — local only, never deployed (GitHub Pages serves everything committed)
AGENTS.md
.opencode/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
.outreach_build
wrangler.jsonc
